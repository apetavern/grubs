@namespace Grubs.UI

@using Grubs.Systems.LevelEditing
@using Grubs.Terrain
@inherits Panel

@if (Level is null)
{
    return;
}

<root>
    
</root>

@code
{
    private static LevelDefinition Level => GameTerrain.Local?.LevelDefinition;

    protected override int BuildHash()
    {
        return HashCode.Combine(Level);
    }
}
