@namespace Grubs.UI

<root>
    <div class="container">
        <label class="title">Players</label>
        <div class="players">
            @foreach (var client in Game.Clients)
            {
                <div class="player">
                    <img src="avatar:@client.SteamId" class="avatar" />
                    <label class="name">@client.Name</label>
                </div>
            }
        </div>
    </div>
    <div style="height: 20px;" />
    <button class="selected @(IsButtonEnabled() ? "" : "disabled")" onclick=@OnStartClick>@GetButtonText()</button>
</root>

@code {
    private string GetButtonText()
    {
        if (!GamemodeSystem.Instance.WorldReady)
            return "GENERATING...";

        if (Game.Clients.Count < GamemodeSystem.Instance.MinimumPlayers)
            return $"PLAYERS {Game.Clients.Count}/{GamemodeSystem.Instance.MinimumPlayers}";

        if (!Game.LocalClient.IsListenServerHost)
            return "WAITING FOR HOST...";

        return "START";
    }

    private void OnStartClick()
    {
        if (IsButtonEnabled())
            StartGame();
    }

    private bool IsButtonEnabled()
    {
        @* Not the best... *@
        return GetButtonText() == "START";
    }

    protected override int BuildHash()
    {
        return HashCode.Combine(Game.Clients.HashCombine(c => c.SteamId), GetButtonText(), IsButtonEnabled());
    }
}

<style>
    PlayMenu {
        top: 40px;
        left: 150px;
        width: 25%;
        flex-direction: column;

        .container {
            height: 600px;
            flex-direction: column;
            align-items: center;
            padding: 10px;

            .title {
                font-size: 50px;
            }

            .players {
                gap: 10px;
                flex-direction: column;
                overflow: scroll;
                width: 100%;

                .player {
                    align-items: center;
                    flex-shrink: 0;

                    .avatar {
                        width: 50px;
                        height: 50px;
                        border-radius: 4px;
                        flex-shrink: 0;
                        margin-right: 5px;
                    }

                    .name {
                        font-size: 40px;
                        white-space: nowrap;
                        text-overflow: ellipsis;
                    }
                }
            }
        }

        button {
            width: 100%;
            height: 100px;
            background-color: #a59441;
            border-width: 4px;
            border-color: #afafaf0c;
            backdrop-filter-blur: 20px;

            label {
                font-size: 3vw;
            }
        }
    }
</style>