@namespace Grubs.UI.Components

@using Grubs.Gamemodes
@using Grubs.Pawn
@using Grubs.Systems.GameMode
@using Grubs.Systems.Pawn

@inherits PanelComponent

<root>
    @if (IsProxy)
        return;

    <Cursor />


    <InputHints />
    <PlayerList />
    @* <InventoryDisplay Inventory=@Inventory /> *@
    <TurnTimer />
</root>

@code
{
    [Property] public required Inventory Inventory { get; set; }

    protected override void OnUpdate()
    {
        base.OnUpdate();

        SetClass("hide", !BaseGameMode.Current.GameStarted);
    }
}